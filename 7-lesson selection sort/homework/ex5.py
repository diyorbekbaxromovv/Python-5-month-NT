def process_names(names):
    result = []
    for name in names:
        result.append(f"{name} layk bosdi")

    return result
name_list = ["Diyorbek", "Javohir", "Suxrob"]
result = process_names(name_list)

for i in result:
    print(i)
